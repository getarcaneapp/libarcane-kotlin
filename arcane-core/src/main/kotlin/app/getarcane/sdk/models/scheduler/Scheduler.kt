package app.getarcane.sdk.models.scheduler

// The Go `types/scheduler` package only defines Go interfaces (`Job`, `ConditionalJob`) for backend
// wiring; there are no HTTP-facing DTOs in this package. This file is intentionally empty so the
// models/scheduler package exists to match the Go layout.
